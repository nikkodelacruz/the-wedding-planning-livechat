<?php 
/**
 * Register custom fields for paypal API
 * @package PaypalParallelPayment
 */

// defined( 'ABSPATH' ) || exit;

class AjaxRequests
{

	// public function __construct(){
	//     add_action( 'wp_ajax_ajax_requests', array($this,'ajax_requests') );
	//     add_action( 'wp_ajax_nopriv_ajax_requests', array($this,'ajax_requests') );
	// }

	// public function ajax_requests()
	// {

		// if( ! DOING_AJAX || ! check_ajax_referer('sample-nonce', 'nonce') ){
		// 	return wp_send_json_error();
		// }

		// $return = array(
		// 	'status' => true
		// );
		// wp_send_json( $return );
		// wp_die();

	// }

}
